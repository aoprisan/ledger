{
  "name": "ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal income, expenses & wishlist — installable PWA",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "gen:icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vite-plugin-pwa": "^1.3.0"
  }
}
